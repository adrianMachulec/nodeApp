<div class="row">
    <div class="col">
        <p>Wyników <%- companies.length %> na <%- resultsCount %></p>
        <nav>
            <ul class="pagination">
            <% for( let i=1; i<=pagesCount; i++ ){ %>
              <li class="page-item <%- page == i ? 'active' : '' %>"><a data-page="<%- i %>" class="page-link" href="#"><%- i %></a></li>
            <% } %>
            </ul>
          </nav>
    </div>
</div>